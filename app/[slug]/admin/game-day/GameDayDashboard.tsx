'use client';

import { useMemo, useState } from 'react';
import { useRouter } from 'next/navigation';
import Link from 'next/link';
import { AlertTriangle, ArrowLeft, ArrowRight, CheckCircle2, Clock3, FileText, Radio, Tv, UserRound, XCircle } from 'lucide-react';
import type { GameDayMatch } from './types';

type Props = { tournamentName: string; tournamentId?: string; slug?: string; date: string; matches: GameDayMatch[] };
const labels = { LIVE: 'En vivo', UPCOMING: 'Próximo', FINISHED: 'Finalizado', UNKNOWN: 'Sin estado' } as const;

function MatchCard({ match }: { match: GameDayMatch }) {
  const stateTone = match.state === 'LIVE' ? 'border-red-200 bg-red-50/60' : match.state === 'FINISHED' ? 'border-slate-200 bg-slate-50' : 'border-blue-100 bg-white';
  return <article className={`rounded-2xl border p-4 shadow-sm ${stateTone}`}><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex items-center gap-2 text-[9px] font-black uppercase tracking-widest text-slate-500"><Clock3 size={14} />{match.scheduledTime?.slice(0, 5) || 'Sin hora'} · {match.venue || 'Sin cancha'}</div><span className={`rounded-full px-3 py-1 text-[9px] font-black uppercase ${match.state === 'LIVE' ? 'bg-red-600 text-white' : match.state === 'FINISHED' ? 'bg-slate-200 text-slate-600' : 'bg-blue-100 text-blue-700'}`}>{match.state === 'LIVE' && <Radio className="mr-1 inline animate-pulse" size={12} />}{labels[match.state]}</span></div><p className="mt-2 text-[9px] font-black uppercase tracking-widest text-slate-400">{match.categoryName} · Jornada {match.roundNumber ?? '-'}</p><div className="mt-4 grid grid-cols-[1fr_auto_1fr] items-center gap-2 text-center"><div className="min-w-0"><div className="mx-auto mb-2 flex h-10 w-10 items-center justify-center rounded-xl bg-white p-1 shadow-sm">{match.homeLogo ? <img src={match.homeLogo} alt="" className="max-h-full max-w-full object-contain" /> : <UserRound className="text-slate-300" size={18} />}</div><p className="truncate text-xs font-black uppercase">{match.homeTeam}</p></div><span className="rounded-lg bg-slate-950 px-3 py-2 text-sm font-black text-white">{match.state === 'UPCOMING' ? 'VS' : `${match.homeScore ?? 0}–${match.awayScore ?? 0}`}</span><div className="min-w-0"><div className="mx-auto mb-2 flex h-10 w-10 items-center justify-center rounded-xl bg-white p-1 shadow-sm">{match.awayLogo ? <img src={match.awayLogo} alt="" className="max-h-full max-w-full object-contain" /> : <UserRound className="text-slate-300" size={18} />}</div><p className="truncate text-xs font-black uppercase">{match.awayTeam}</p></div></div><div className="mt-4 flex flex-wrap items-center justify-between gap-2 border-t border-slate-200/70 pt-3"><div className="text-[9px] font-bold uppercase text-slate-500">{match.scorekeeper ? `Planillero: ${match.scorekeeper}` : 'Planillero pendiente'}</div><div className="flex flex-wrap gap-2"><a href={match.hrefs.mesa} className="flex min-h-10 items-center gap-1 rounded-lg bg-slate-950 px-3 text-[9px] font-black uppercase tracking-wider text-white"><ArrowRight size={13} /> Mesa</a><a href={match.hrefs.planilla} className="flex min-h-10 items-center gap-1 rounded-lg border border-amber-200 bg-amber-50 px-3 text-[9px] font-black uppercase tracking-wider text-amber-700"><FileText size={13} /> Planilla</a><a href={match.hrefs.tv} target="_blank" rel="noreferrer" aria-label="Abrir TV" className="flex min-h-10 items-center gap-1 rounded-lg border border-indigo-200 bg-indigo-50 px-3 text-[9px] font-black uppercase tracking-wider text-indigo-700"><Tv size={13} /> TV</a></div></div>{match.alerts.length > 0 && <div className={`mt-3 flex items-start gap-2 rounded-xl p-3 text-[9px] font-black uppercase ${match.readiness === 'BLOCKED' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-800'}`}><AlertTriangle size={14} className="shrink-0" />{match.alerts.join(' · ')}</div>}</article>;
}

export default function GameDayDashboard({ tournamentName, tournamentId, slug, date, matches }: Props) {
  const router = useRouter();
  const onDateChange = (nextDate: string) => router.replace(`/${slug || ''}/admin/game-day?tournament=${encodeURIComponent(tournamentId || '')}&date=${encodeURIComponent(nextDate)}`, { scroll: false });
  const [filter, setFilter] = useState<'ALL' | 'LIVE' | 'UPCOMING' | 'FINISHED' | 'ALERTS'>('ALL');
  const visible = useMemo(() => matches.filter((match) => filter === 'ALL' || filter === match.state || (filter === 'ALERTS' && match.alerts.length > 0)), [matches, filter]);
  const counts = { live: matches.filter((m) => m.state === 'LIVE').length, upcoming: matches.filter((m) => m.state === 'UPCOMING').length, finished: matches.filter((m) => m.state === 'FINISHED').length, alerts: matches.filter((m) => m.alerts.length > 0).length };
  const backHref = `/${slug || 'demo-7c9f3a-sportscore'}/admin${tournamentId ? `?tournament=${encodeURIComponent(tournamentId)}` : ''}`;
  return <main className="min-h-screen bg-slate-50 p-4 text-slate-950 sm:p-6 lg:p-8"><div className="mx-auto max-w-7xl space-y-6"><header className="rounded-3xl bg-slate-950 p-5 text-white shadow-xl sm:p-7"><div className="mb-5 flex items-center justify-between gap-3"><Link href={backHref} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/15 bg-white/10 px-4 text-[10px] font-black uppercase tracking-widest text-white transition-all hover:bg-white/20 focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-blue-400"><ArrowLeft size={16} /> Menú principal</Link><span className="hidden text-[10px] font-black uppercase tracking-[.2em] text-slate-400 sm:inline">Centro de operaciones</span></div><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.25em] text-blue-400">Centro de operaciones · Game Day</p><h1 className="mt-2 text-3xl font-black uppercase tracking-tight sm:text-5xl">Jornada operativa</h1><p className="mt-2 text-sm font-semibold text-slate-300">{tournamentName}</p></div><label className="flex flex-col gap-2 text-[9px] font-black uppercase tracking-widest text-slate-400">Fecha de operación<input aria-label="Fecha de operación" type="date" value={date} onChange={(event) => onDateChange(event.target.value)} className="h-12 rounded-xl border border-white/10 bg-white px-4 text-sm font-black text-slate-900 outline-none focus-visible:ring-4 focus-visible:ring-blue-400" /></label></div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><Stat icon={Radio} label="En vivo" value={counts.live} tone="red" /><Stat icon={Clock3} label="Próximos" value={counts.upcoming} tone="blue" /><Stat icon={CheckCircle2} label="Finalizados" value={counts.finished} tone="slate" /><Stat icon={AlertTriangle} label="Alertas" value={counts.alerts} tone="amber" /></div></header><div className="flex gap-2 overflow-x-auto pb-1" aria-label="Filtros de Game Day">{[['ALL','Todos'],['LIVE','En vivo'],['UPCOMING','Próximos'],['FINISHED','Finalizados'],['ALERTS','Con alertas']].map(([value,label]) => <button key={value} type="button" aria-pressed={filter === value} onClick={() => setFilter(value as typeof filter)} className={`min-h-11 shrink-0 rounded-xl px-4 text-[9px] font-black uppercase tracking-widest focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-blue-200 ${filter === value ? 'bg-blue-600 text-white' : 'border border-slate-200 bg-white text-slate-500'}`}>{label}</button>)}</div>{visible.length ? <div className="grid gap-4 lg:grid-cols-2">{visible.map((match) => <MatchCard key={match.id} match={match} />)}</div> : <section className="rounded-3xl border border-dashed border-slate-300 bg-white p-12 text-center"><XCircle className="mx-auto text-slate-300" size={32} /><h2 className="mt-3 text-lg font-black uppercase">No hay partidos para esta fecha</h2><p className="mt-2 text-sm font-semibold text-slate-400">Prueba otra fecha o cambia el filtro operativo.</p></section>}</div></main>;
}

function Stat({ icon: Icon, label, value, tone }: { icon: typeof Radio; label: string; value: number; tone: 'red' | 'blue' | 'slate' | 'amber' }) { const styles = { red: 'bg-red-500/15 text-red-200', blue: 'bg-blue-500/15 text-blue-200', slate: 'bg-white/10 text-slate-200', amber: 'bg-amber-500/15 text-amber-200' }; return <div className={`rounded-2xl p-3 ${styles[tone]}`}><Icon size={17} /><p className="mt-2 text-2xl font-black text-white">{value}</p><p className="text-[9px] font-black uppercase tracking-widest">{label}</p></div>; }
